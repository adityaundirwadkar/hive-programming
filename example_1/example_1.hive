SELECT RATINGS.MOVIE_ID,AVG(RATING) AVGRATING
FROM RATINGS INNER JOIN MOVIES ON MOVIES.MOVIE_ID=RATINGS.MOVIE_ID
WHERE (INSTR(MOVIES.GENRES,"Action")>0)
GROUP BY RATINGS.MOVIE_ID
ORDER BY AVGRATING DESC
LIMIT 10;